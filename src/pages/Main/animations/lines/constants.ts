const fullProgress: number = 2000;

const fullProgressStart: number = 500;

const titleFullProgress: number = 100;

const startCalculationAnimation: number = 8

const resetStoreAfterPercentage: number = 10

export {
    fullProgress,
    fullProgressStart,
    titleFullProgress,
    startCalculationAnimation,
    resetStoreAfterPercentage
}